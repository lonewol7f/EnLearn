package net.enLearn.dao;

/**
 * Created by Osini Kithma
 * 8/25/21
 */

//@Repository
public class FreeQuizDAOImpl /*<Grade> implements FreeQuizDAO */{

/*    @Autowired
    private SessionFactory sessionFactory;

    @Override
    public List<FreeQuiz> getFreeQuizListById(int id){
        // get current session
        Session session = sessionFactory.getCurrentSession();

        //get Quiz associated with grade
        Grade grade = new Grade();
        List<FreeQuiz> freeQuizList = grade.getFreeQuizzes();

        return freeQuizList;

    }

    @Override
    public void saveOrUpdateFreeQuiz(FreeQuiz freeQuiz){
        // get current session
        Session session = sessionFactory.getCurrentSession();

        // if id associated with object, it will save
        session.saveOrUpdate(freeQuiz);
    }

    @Override
    public void deleteFreeQuiz(int FreeQuizId){
        // get current session
        Session session = sessionFactory.getCurrentSession();

        String freeQuiz = new String();
        session.delete(freeQuiz);
    }

 */

}
